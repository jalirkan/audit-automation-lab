# python
__pycache__/
*.pyc
.venv/

# local secrets (no keys should ever be needed, but belt and braces)
.env
.env*.local

# generated ledgers and runs (examples/ stays committed)
/runs/
/generated/

# example raw ledgers are large and regenerate byte-identically from the
# seed (python cli.py example); the manifest, flags and pack stay committed
examples/**/ledger.json
examples/**/ledger.csv
*.db
*.db-wal
*.db-shm

# os
.DS_Store
